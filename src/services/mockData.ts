import { InventoryItem, InventoryLog } from '../types/inventory';

// Clean initial empty inventory state (no seeded sample records)
export const INITIAL_MOCK_ITEMS: InventoryItem[] = [];

export const INITIAL_MOCK_LOGS: InventoryLog[] = [];
